const LineGradient = ({ width = "w-full" }) => {
  return <div className={`h-0.5 ${width} bg-gradient-blue`} />;
};

export default LineGradient;

// este componente representa una línea horizontal con un fondo de degradado azul, y puedes ajustar su ancho utilizando el prop width.
